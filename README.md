# Sales-Database-Analysis-Department
This SQL analytics project processes customer, product, and sales data using MySQL. Key implementations include schema exploration, filtering (WHERE, IN, BETWEEN), NULL handling, conditional logic (CASE), and sorting. It utilizes GROUP BY, HAVING, and multi-table JOINs to deliver high-level management reports.
